// Exemple de 2 fonctions et 1 variable
const print = (text) => console.log(text);
const add = (n1, n2) => n1 + n2;

const variable = "variable example";

// Exportation à la fin du fichier
module.exports = { print, add, variable };
